# LuminaJS Angular Demo

This example demonstrates the standalone `LuminaCanvasComponent` exported from `@gks101/luminajs/angular`.

```bash
npm install
npm run dev
```

The demo imports the local source through `tsconfig.json` paths so changes in `src/angular` can be tested while developing this repo.
