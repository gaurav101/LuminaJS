{
  "name": "luminajs-core",
  "root": "src",
  "sourceRoot": "src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/core"],
      "options": {
        "commands": [
          {
            "command": "npx tsc -p tsconfig.core.json"
          }
        ]
      }
    }
  }
}
