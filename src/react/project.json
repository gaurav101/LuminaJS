{
  "name": "luminajs-react",
  "root": "src/react",
  "sourceRoot": "src/react",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/react"],
      "options": {
        "commands": [
          {
            "command": "npx tsc -p tsconfig.react.json"
          }
        ]
      }
    }
  }
}
